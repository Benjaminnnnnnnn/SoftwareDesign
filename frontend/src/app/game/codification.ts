import { Board } from "./board";
import Piece from "./pieces/piece";

export function encodeBoardToString(b: Board) {
  const piecesToEncode = [];
  const piecesOnBoard = b.tiles.values();
}

export function decodeStringToBoard(s: string) {}
