// Render the current game state

import { useState, useEffect, useRef } from "react";

export default function GameRenderer() {
  const [canvas, setCanvas] = useState(document.getElementById("c"));
  return <div> Hello </div>;
}
